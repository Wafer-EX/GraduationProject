<div class="slider-container">
    <button class="slider-button slider-left-button" @onclick=Next>←</button>

    @if (selected == 1)
    {
        <div class="slider-content-cont">
            <div class="slider-content">
                <img class="slider-icon" src="icons/speed-icon-white.png"/>
                <div>
                    <div class="slider-title">Скорость</div>
                    <div class="slider-text">Быстро согласуем техническое задание, быстро реализуем проект.</div>
                </div>
            </div>
            <img class="slider-image" src="images/slider/slider1.jpg"/>
        </div>
    }
    else if (selected == 2)
    {
        <div class="slider-content-cont">
            <div class="slider-content">
                <img class="slider-icon" src="icons/quality-icon-white.png"/>
                <div>
                    <div class="slider-title">Качество</div>
                    <div class="slider-text">Наши специалисты благодаря своей высокой квалификации способны качественно разработать ваш проект.</div>
                </div>
            </div>
            <img class="slider-image" src="images/slider/slider2.jpg"/>
        </div>
    }
    else if (selected == 3)
    {
        <div class="slider-content-cont">
            <div class="slider-content">
                <img class="slider-icon" src="icons/support-icon-white.png"/>
                <div>
                    <div class="slider-title">Поддержка</div>
                    <div class="slider-text">Обратитесь в службу поддержки по номеру телефона, и ваша проблема будет решена нашими специалистами.</div>
                </div>
            </div>
            <img class="slider-image" src="images/slider/slider3.jpg"/>
        </div>
    }

    <button class="slider-button slider-right-button" @onclick=Next>→</button>
</div>

@code {
    private int selected = 1;

    private int Selected
    {
        get => selected;
        set
        {
            if (value > 3) selected = 1;
            else if (value < 1) selected = 3;
            else selected = value;
        }
    }

    private void Next()
    {
        Selected++;
        StateHasChanged();
    }

    private void Prev()
    {
        Selected--;
        StateHasChanged();
    }
}